# TruLens 单环境仪表盘依赖调整任务

关联设计文档：`docs/spec/spec_20260910_184651_TruLens单环境仪表盘依赖调整.md`

## 任务状态

- [x] 检查候选 unstructured 版本的 psutil 依赖并确定精确版本。
- [x] 更新单环境评测依赖，移除独立 Dashboard 依赖文件。
- [x] 在 yyy-rag 环境中安装并验证统一依赖。
- [x] 更新 Dashboard 运行说明与配置测试。
- [x] 验证 PostgreSQL 连接、Dashboard 数据库解析、评测单测和文档解析测试。
- [x] 更新任务记录；不执行 Git 提交。

## 实施约束

- 不修改 `src/`。
- 禁止使用 `--no-deps`、强制覆盖或保留 `pip check` 冲突。
- 仅保留 `yyy-rag` 一个项目运行环境；删除项目内独立 Dashboard 依赖文件。
- 不删除用户已创建的 Conda 环境，除非用户后续明确要求。
- 不执行 Git 提交。

## 验证记录

- [x] 依赖元数据检查：`unstructured==0.18.32` 仅要求 `psutil`，可与 `trulens-dashboard==2.14.0` 的 `psutil<6` 共存；`unstructured==0.20.8` 及以后版本要求 `psutil>=7.2.2`，不兼容。
- [x] `yyy-rag` 环境已统一安装 `trulens-dashboard==2.14.0`、`unstructured==0.18.32`、`psutil==5.9.8`、`psycopg==3.3.5`。
- [x] `python -m pip check`：`No broken requirements found.`
- [x] `python -m pytest test/evaluation -q --basetemp .pytest_tmp`：12 passed。
- [x] `SimpleDirectoryReader` 成功读取 `test/fixtures/sample.txt`：1 条文档、101 字符。
- [x] 在单环境下运行 `test_doc_rag_with_trulens`：1 passed；真实 RAG 记录继续写入 PostgreSQL。
- [x] 主环境 `python -m trulens.dashboard --find` 成功解析 PostgreSQL `trulens` 数据库。
- [x] 主环境启动的 Dashboard 正在监听 `127.0.0.1:8501`，HTTP 状态为 200。
- [x] `git diff --name-only -- src`：无输出，确认未修改生产代码。

## 已知限制

- 项目源码目前没有直接调用 `unstructured`。若以后直接调用 `unstructured.partition.text`，需要离线预置 NLTK 的 `punkt_tab` 等数据；本地环境通过代理自动下载时会被安全策略拒绝。
- TruLens 的 LLM-as-a-Judge 为异步评分。本次真实评测结束时出现 `Rating must be in [0, 3]` 的后台评分格式告警；请求、上下文、回答和追踪记录已写入 PostgreSQL，但对应评分列可能为空。该问题与本次单环境依赖调整无关，建议后续单独检查裁判模型的评分输出格式。
- 先前创建的 `yyy-rag-trulens-dashboard` Conda 环境已不再被项目使用；本次未删除该环境，避免未经确认移除本机资源。
- 未执行 Git 提交，是否提交由用户决定。

## 后续修复记录

- [x] 2026-09-10：修复 Dashboard 子进程错误命中 `PATH` 中 PATH 中的其他环境 `streamlit.exe` 的问题。启动器现强制使用 `yyy-rag` 的 `python.exe -m streamlit`，不会依赖外部 `streamlit.exe`。
- [x] 增加启动命令回归测试；`python -m pytest test/evaluation -q --basetemp .pytest_tmp`：13 passed。
- [x] 验证 Dashboard 子进程命令以 当前 Python 环境的 `python -m streamlit` 开始，并且 `http://127.0.0.1:8501` 返回 HTTP 200。

## 注释补充

- [x] 2026-09-10：为 `DocRagEvaluationAdapter.run()` 补充中文分步注释，说明检索、上下文提取、格式化、生成回答与 TruLens 记录的职责；未改变执行逻辑。

## 异步指标断言修复

- [x] 2026-09-10：修复 `test_doc_rag_with_trulens` 将 `get_records_and_feedback()` 的异步结果列错误当作指标注册状态的问题。
- [x] 改为直接验证 `recorder.feedbacks` 中注册的 `Context Relevance`、`Answer Relevance`、`Groundedness` 三项指标。
- [x] 原始集成测试在 PostgreSQL 配置下通过：`1 passed`。

## 裁判评分范围修复

- [x] 2026-09-11：替换 TruLens 默认自由文本 CoT 评分解析，改为严格 JSON 协议：`{"score": 0..3, "reason": "..."}`。
- [x] 本地解析器拒绝非 JSON、缺少字段、非整数以及范围外评分；首次失败时以纠错提示重试一次，第二次仍失败时明确记录异常，不再产生默认解析器的范围错误。
- [x] 裁判模型请求设置 `timeout=30`、`max_retries=1`，避免评分调用无限阻塞。
- [x] 单元测试覆盖“首次返回 4 分、第二次返回 3 分”的重试场景；`python -m pytest test/evaluation -q --basetemp .pytest_tmp`：15 passed。
- [x] 真实 DashScope 裁判调用通过严格 JSON 校验并返回合法原始评分 `0`。

## 完整 RAG Triad 执行修复

- [x] 2026-09-11：定位到 TruLens 默认后台 evaluator 会扫描 PostgreSQL 中全部历史未完成 record，并与当前 record 的同步计算争用锁，导致 pytest 退出前只完成部分指标。
- [x] Recorder 设置 `start_evaluator=False`；完整评测仅通过 `recording.retrieve_feedback_results()` 同步计算和等待当前 record 的三项指标。
- [x] 回归测试覆盖后台 evaluator 禁用和当前 record 反馈等待。
- [x] 已获授权执行真实评测：record `dcecc775-4b0e-45e3-80ba-1025f8ebf1be` 完整写入 PostgreSQL，Context Relevance=0.133333、Answer Relevance=0.333333、Groundedness=0.666667。


## 2026-09-11 ????????

- [x] ?? `rag-framework-purpose` ?????Context Relevance ? Answer Relevance ????Groundedness ?????? 30 ???????
- [x] ?????????? 30 ???? 90 ???? `max_retries=1`???? 0 ? 3 ??????
- [x] ?????????1 passed??? record ? Context Relevance?Answer Relevance?Groundedness ???? PostgreSQL??????? 0?
- [x] `python -m pytest test/evaluation -q --basetemp .pytest_tmp`?17 passed?
- [x] `python -m pip check`?No broken requirements found?

