# TruLens RAG 评估接入任务

关联设计文档：`docs/spec/spec_20260910_163617_TruLensRAG评估接入.md`

## 任务状态

- [x] 新增独立评估依赖清单与 pytest `integration` 标记配置。
- [x] 新增 JSONL 最小评测集。
- [x] 新增生产链路外的 TruLens RAG 评估适配器。
- [x] 新增评估适配器的单元测试，并完成 Red-Green 验证。
- [x] 新增显式触发的 TruLens 集成评估测试。
- [x] 运行静态编译、评估单测与现有非远程测试进行验证。
- [x] 记录运行命令、已知成本与评估限制。

## 任务 1：评估基础设施

**文件：** 新增 `requirements-evaluation.txt`、`pytest.ini`、`test/evaluation/__init__.py`。

- [x] 固定 `trulens-core==2.14.0` 与 `trulens-providers-langchain==2.14.0`，禁止引入会降级 OpenAI SDK 的 `trulens-providers-openai`。
- [x] 注册 `integration` pytest 标记，使远程评估仅能由显式命令触发。

## 任务 2：独立适配器与评测集

**文件：** 新增 `src/evaluation/trulens_doc_rag_adapter.py`、`RAG-assessment/data/doc_rag_eval.jsonl`。

- [x] 定义 `DocRagEvaluationAdapter`，构造时注入现有 embedding、Milvus、LLM 及可替换的检索函数。
- [x] 将检索和生成拆为可追踪方法；生产代码不添加 TruLens 依赖。
- [x] 返回包含 `question`、`contexts` 和 `answer` 的结构化记录。
- [x] JSONL 每行包含 `id` 和 `question`；加载时验证字段、空值与重复 ID。

## 任务 3：测试驱动验证

**文件：** 新增 `test/evaluation/test_trulens_doc_rag_adapter.py`、`test/integration/test_trulens_doc_rag.py`。

- [x] 先编写适配器与评测集加载的失败单元测试，再实现最小代码使其通过。
- [x] 单元测试使用伪造检索函数和伪造 LLM，不调用 DashScope、MinerU 或 Milvus。
- [x] 集成测试标记为 `integration`，显式读取 JSONL 并运行 TruLens 三项指标。
- [x] 集成测试在缺少 TruLens 或远程配置时给出明确跳过原因，绝不泄露凭据。

## 任务 4：验证与说明

**文件：** 新增 `RAG-assessment/README.md`，更新本任务文档。

- [x] 说明依赖安装、单元测试、显式集成评估与查看本地记录的命令。
- [x] 说明裁判模型调用成本、数据出站风险与不在普通 pytest 中运行的原因。
- [x] 运行 Python 编译、评估单测及现有非远程测试。
- [x] 每个完成项立即在本文件标记为 `[x]`。

## 实施约束

- 不修改 `src/` 生产代码。
- 只在显式执行 `-m integration` 时运行远程模型评测。
- 不打印 API Key、MinerU Token 或 Authorization 请求头。
- 不执行 Git 提交；由用户决定是否提交。

## 验证记录

- [x] `python -m compileall -q src test`：通过。
- [x] `python -m pytest test/evaluation/test_trulens_doc_rag_adapter.py -q --basetemp .pytest_tmp`：5 passed，包含 UTF-8 BOM JSONL 回归测试。
- [x] `python -m pytest --collect-only -q`：成功收集 8 项测试。
- [x] `git diff --name-only -- src`：无输出，确认未修改生产代码。

## 已知限制

- [x] `RUN_TRULENS_EVAL=1`、`TRULENS_CASE_LIMIT=1` 下执行 `test_doc_rag_with_trulens`：1 passed，真实 DashScope、Milvus 与 TruLens 链路已验证。
- 测试收集和评估测试会显示 DashScope Assistants API 的弃用告警；该告警来自 DashScope 包导入，与本次 TruLens 接入无直接关系。
- 未执行 Git 提交，是否提交由用户决定。


