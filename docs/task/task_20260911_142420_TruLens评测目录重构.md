# TruLens 评测目录重构任务

关联设计文档：`docs/spec/spec_20260911_142420_TruLens评测目录重构.md`

## 任务状态

- [x] 迁移 TruLens 运行时模块到 src/evaluation/。
- [x] 创建 RAG-assessment/ 评测脚本与数据目录。
- [x] 更新测试导入、评测集路径和 Dashboard 启动入口。
- [x] 将历史 spec/task 文件集中到 docs/spec/ 与 docs/task/。
- [x] 更新 README 和历史文档引用。
- [x] 运行编译、评测单测和测试收集验证。
- [x] 更新本任务文档；不执行 Git 提交。

## 实施约束

- 不修改 src/query/、src/index/、src/infra/ 等生产 RAG 模块。
- 大文件分批写入，避免单次写入超时。
- 不执行 Git 提交，由用户决定。


## 验证记录

- [x] TruLens 运行时实现已迁移到 `src/evaluation/`。
- [x] 评测脚本、Dashboard 入口和评测集已迁移到 `RAG-assessment/`。
- [x] 测试导入、评测集路径和 Dashboard 启动入口已更新。
- [x] 历史 spec/task 已集中到 `docs/spec/` 与 `docs/task/`，任务文档链接已更新。
- [x] `python -m compileall -q src test RAG-assessment`：通过。
- [x] `python -m pytest test/evaluation -q --basetemp .pytest_tmp`：18 passed。
- [x] `python -m pytest --collect-only -q`：22 tests collected。
- [x] 旧实现路径扫描无结果。
- [x] `git diff --name-only -- src/query src/index src/infra`：无输出，确认既有生产 RAG 模块未修改。
- [x] 未执行 Git 提交，是否提交由用户决定。



## 文档命令泛化

- [x] 2026-09-11：将 `RAG-assessment/README.md` 中的机器特定绝对路径统一改为项目根目录下可执行的相对命令，例如 `python -m pip`、`python RAG-assessment/run_trulens_evaluation.py` 和 `python RAG-assessment/run_trulens_dashboard.py`。
- [x] 移除评测相关文档中用户目录、Python 安装目录和项目绝对路径的展示；历史验证记录改为通用环境描述。
