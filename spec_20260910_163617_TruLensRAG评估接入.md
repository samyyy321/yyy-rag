# TruLens RAG 评估接入设计

## 目标

在不改动 `src/` 生产 RAG 链路的前提下，引入 TruLens，对当前文档 RAG 的检索上下文与生成回答执行可重复的离线评估，并将评估记录保存在本地 TruLens 数据库中。

## 范围

### 包含

- 新增独立 TruLens 评估适配器，调用现有 `search_docs_raw`、`format_doc_context`、`get_llm` 和 `DOC_QA_PROMPT`。
- 新增最小 JSONL 评测集，包含具有唯一 ID 的问题。
- 评估 Context Relevance、Answer Relevance 与 Groundedness。
- 新增可显式执行的集成评估测试；默认普通 pytest 不自动执行远程 LLM 评测。
- 新增独立依赖清单与运行说明。

### 不包含

- 不修改 `src/query/doc_rag.py`、`src/query/reranker.py`、`src/infra/` 或 Milvus schema。
- 不改变生产请求路径、检索数量、提示词或模型配置。
- 不自动在 CI、普通单元测试或线上请求中运行 LLM-as-a-Judge。
- 不提交 Git。

## 架构

评估代码位于 `test/evaluation/`，作为生产代码的外层调用者。适配器执行与当前 RAG 一致的顺序：调用 `search_docs_raw` 获取最终片段，调用 `format_doc_context` 生成上下文，用当前 `DOC_QA_PROMPT` 和 `get_llm()` 生成回答。TruLens 仅包裹适配器实例，记录输入、检索上下文和输出；生产模块不感知 TruLens。

评估使用 `trulens-core` 与 `trulens-providers-langchain`，通过现有 `ChatOpenAI` 实例作为裁判模型。这避免安装 `trulens-providers-openai`：后者要求 `openai<2`，会与本项目 `langchain-openai==1.6.2` 所要求的 `openai>=2.45,<4` 冲突。

若设置 `TRULENS_EVAL_MODEL`，该配置只用于创建裁判模型，优先级高于 `LLM_MODEL`；未设置时裁判模型复用当前 `LLM_MODEL`。该字段不会改变生产回答模型。

## 指标

- Context Relevance：问题与每个检索片段的相关性。
- Answer Relevance：问题与最终回答的相关性。
- Groundedness：回答中的陈述是否由检索上下文支持。

首版不要求标准答案；后续可在 JSONL 增加 `expected_answer` 后扩展基于 Ground Truth 的评估。

## 数据、成本与安全

- 默认使用本地 TruLens 数据库保存记录，不上传评测数据到 TruLens 云服务。
- DashScope 会接收问题、检索片段和生成回答用于裁判评分，因此评测集不得包含敏感数据。
- 评估是显式触发的远程集成测试，会产生模型调用成本。
- 评测输出不得记录或打印 API Key、Token 与完整 Authorization 头。

## 验收标准

1. `src/` 无 TruLens import 或改动。
2. 无 TruLens 依赖时，普通现有测试仍可收集和运行。
3. 安装评估依赖后，评估测试可加载 JSONL 问题集，且仅在显式标记下运行。
4. 适配器能记录 question、contexts 与 answer，并配置三项反馈指标。
5. 评估记录可由本地 TruLens Session 查询；失败信息包含可定位原因而不泄露凭据。
